# README and Repository Publication Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Publish the current FINO-Net work to the requested GitHub repository with an accurate, usable README.

**Architecture:** Keep the existing training code unchanged. Replace the minimal project landing page with documentation derived from the checked-in dataset layout and training scripts, then commit the documentation and intended untracked visual asset before pushing `main` to the user-specified remote.

**Tech Stack:** Markdown, Git, Python, PyTorch, ConvLSTM.

## Global Constraints

- Preserve all existing tracked project files and do not add generated Python bytecode.
- Document only commands and dependencies evidenced by the repository.
- Push `main` to `https://github.com/Ocean-645-SSS/my-fino-net.git` after local checks succeed.

---

### Task 1: Document project usage

**Files:**
- Modify: `README.md`
- Add: `.gitignore`
- Add: `assets/fino-net-logo.png`

**Interfaces:**
- Consumes: model and data-loading behavior in `src/train_autodl_0.py`
- Produces: setup, dataset, training, and citation guidance for repository visitors

- [ ] **Step 1: Record verified project facts**

Read `src/train_autodl_0.py`, `src/convlstm.py`, `annotation/`, and `assets/` to establish the RGB, depth, audio, action, and package references used in the documentation.

- [ ] **Step 2: Replace the landing-page README**

Add a Chinese overview, model/data description, repository layout, environment setup, data-path configuration, training command, experiment-script note, and upstream paper citation. Embed `assets/fino-net-logo.png` as the project visual. Add `.gitignore` rules for Python bytecode, virtual environments, notebook checkpoints, and local model artifacts.

- [ ] **Step 3: Check Markdown links and commands**

Run: `rg -n "https?://|python src/train_autodl_0.py|assets/fino-net-logo.png" README.md`

Expected: the README contains the official paper/data references, the selected training entry point, and the checked-in logo path.

### Task 2: Commit and publish the current repository state

**Files:**
- Modify: `.git/config` (remote URL only; Git metadata, not committed)
- Commit: `README.md`, `.gitignore`, `assets/fino-net-logo.png`, and this execution plan

**Interfaces:**
- Consumes: a clean verification result and the user-provided GitHub repository URL
- Produces: a `main` branch published to `Ocean-645-SSS/my-fino-net`

- [ ] **Step 1: Exclude generated bytecode from staging**

Verify `src/__pycache__/` remains untracked and is not included in the commit.

- [ ] **Step 2: Create a focused commit**

Run: `git add README.md .gitignore assets/fino-net-logo.png docs/superpowers/plans/2026-09-08-readme-and-publish.md && git commit -m "docs: improve project README"`

Expected: one commit containing the README, repository-ignore rules, logo, and execution plan only.

- [ ] **Step 3: Point origin at the requested repository and push**

Run: `git remote set-url origin https://github.com/Ocean-645-SSS/my-fino-net.git && git push -u origin main`

Expected: GitHub accepts `main` and reports its upstream as `origin/main`.
